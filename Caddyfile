# osliceinspiration.com → company website
{$DOMAIN} {
  encode gzip
  reverse_proxy company-website:4000
}

# agent.osliceinspiration.com → AI Agent terminal
{$AGENT_DOMAIN} {
  encode gzip
  reverse_proxy agent-os:8000
}
